import { render } from '@wordpress/element';
import DeviceRedirectSettings from './components/DeviceRedirectSettings';

render(
    <DeviceRedirectSettings />,
    document.getElementById('device-redirect-settings')
);